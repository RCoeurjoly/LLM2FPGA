* LLM2FPGA Task 6 State of the Art
** Snapshot
- last_updated: 2026-06-20T11:58:20+02:00
- milestone: M2.4-live-context-attention-slice
- build_variant: =default/latest-passing-milestone is .#task6-ypcb-pcie-rowstream-ingress-m1-pnr100-bitstream; bottleneck remains the M2 path. Latest accepted focused M2.3 bitstream is copied to artifacts/task6/bitstreams/task6-m2-3-embedding-handoff-pnr100-2026-06-19.bit, sha256=955aacdf819cefc2bf0e631cde6ebecae42f0972691310661225d65b9e5ffea8. Current M2.4 seed17 candidate has restored RTL-side input BAR lane compensation but missed final pcie_user_clk timing.=
- next_milestone: M2.4-live-context-attention-slice
- latest_green_artifact:
  =artifacts/task6/runs/task6-bottleneck/m2-3-embedding-handoff-output-hash-proof.json=
- current_gap: M1 remains the default/latest-passing external build. M2 remains
  the bottleneck, split into M2.1-M2.7 submilestones. M2.1, M2.2, and M2.3 are
  HIL-proven on earlier focused images. Current M2.4 work restored RTL-side
  input BAR lane compensation and sim-proved direct input/residual identity,
  but the seed17 M2.4 bitstream missed final timing, so M2.4 BAR identity is
  not HIL-proven on the current candidate.
- failure_signature: =M2.4 seed17 with restored input BAR compensation: rowstream ingress sim PASS for direct input/residual identity after complete 64-bit lane writes. Bitstream build reached legal routing but failed timing: pcie_user_clk 59.53 MHz below the 62.50 MHz target. Critical path is in the M2.4 LN/context logic from ln_center_flat_index_w[6] through context_i to ln_mean_input_q12_q[0], about -0.8 ns worst slack. No M2.4 HIL or BAR gate was run from this non-timing-clean build. Latest accepted green artifact remains M2.3: artifacts/task6/runs/task6-bottleneck/m2-3-embedding-handoff-output-hash-proof.json.=
- next_action: Make a timing-clean M2.4 candidate, then flash/recover to
  =pcie_ready=, run a BAR-only public-interface probe for direct input and
  residual identity, and only then run the M2.4 board gate. If any HIL gate
  fails, reproduce the public-interface failure in simulation; if sim passes
  while HIL fails, extend the simulation/diagnostic boundary before changing
  milestone status.
- status: M1_HIL_GREEN_M2_1_HIL_GREEN_M2_2_HIL_GREEN_M2_3_HIL_GREEN_M2_4_TIMING_BLOCKED
- priority: Use the HIL-proven M1 baseline as the default/latest-passing
  milestone; pursue M2 full-block HIL as the bottleneck toward TinyStories-1M
  inference.
- baseline_bundle:
  =artifacts/task6/baselines/tiny-stories-1m-baseline-float-selftest-all-memory-utilization=

** Interpretation
- The main completion path is still TinyStories-1M inference on YPCB.
- =nix build= now represents the HIL-proven M1 latest-passing milestone.
- =nix build .#bottleneck= represents the current unsolved M2 HIL milestone.
- Board claims require JSON/run artifacts under =artifacts/task6/runs/=.

** M2 Ladder
- M2.0 artifact discipline: accepted compute submilestones require
  live-compute, direct-mode, strict-readback artifacts. Transport and lifecycle
  submilestones are explicitly non-compute evidence and must say so in their
  contract.
- M2.1 PCIe/BAR contract: direct host writes read back identity for
  input/residual/control without compensated host tricks.
- M2.2 start/clear lifecycle: start_count increments, DONE/ERROR is stable,
  clear returns IDLE, no all-ones/stale BAR.
- M2.3 embedding and block input handoff: host token IDs produce expected block
  input vector and LN input checksum.
- M2.4 live context/attention slice: LN + QKV + softmax/value/context vector
  matches oracle.
- M2.5 attention out-proj + residual + LN2: vector boundaries match oracle.
- M2.6 MLP + final residual: c_fc/GELU/c_proj/final residual match oracle.
- M2.7 full block direct-mode acceptance: one complete TinyStories block from
  token IDs/control to final output matches.

** M3 Ladder
- M3.1 inter-block state movement / DDR3 rowstream contract.
- M3.2 repeated-block execution for a tiny fixed token sequence.
- M3.3 full TinyStories-1M prompt, token-exact greedy output.
- M3.4 repeatability run: same prompt twice after clear/recover.

** Acceptance criteria
*** G1 latest green
- [X] =nix build= succeeds.
- [X] =nix build .#latest-passing-milestone= succeeds.
- [X] =just task6-latest-passing-milestone-board-gate= equivalent passes or records a
  precise hardware failure signature.
- [X] =latest_green_artifact= points at the newest accepted PASS artifact.

*** G2 bottleneck
- [X] =nix build .#bottleneck= succeeds.
- [X] =just task6-bottleneck-board-gate= equivalent ran with explicit output.
- [X] =current_gap= states the exact failing delta from latest green.
- [X] =next_action= is a single concrete command or diagnostic step.

** Machine-readable view
- JSON companion: [[file:task6-crisp-state.json]]
