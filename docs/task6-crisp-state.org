* LLM2FPGA Task 6 State of the Art
** Snapshot
- last_updated: 2026-06-18T00:00:00+02:00
- milestone: G1-latest-green
- build_variant: =nix build -> .#latest-passing-milestone -> task6-int8-v9984-l2-residual-add-output-head-selftest-jtag-debug-5mhz.bit; HIL gate -> just task6-latest-passing-milestone-board-gate=
- next_milestone: G1-latest-green-hil-pass
- latest_green_artifact:
- current_gap: The latest-passing milestone builds and programs on hardware, but
  it is not HIL-proven because the board gate fails at DDR3 loader readiness
  during boot-only.
- failure_signature: =artifacts/task6/runs/2026-06-18-latest-passing-hil/gate-summary.json: status FAIL, boot_gate FAIL, TimeoutError loader did not become ready: magic_ok=True version=13 calib_seen=True state=12 ack=14071040 err=14071040 loader_error=False debug1=0xe500e500. Program log reports DONE with isc_done=1 init=1 done=1.=
- next_action: Debug why the programmed latest-passing bitstream reaches
  =calibration_seen= but never loader-ready state 12; rerun
  =just task6-latest-passing-milestone-board-gate= with an explicit run root
  after the fix.
- status: BLOCKED_LATEST_PASSING_HIL
- priority: DDR3-first TinyStories-1M inference; BRAM-only targets are
  diagnostics/regressions.
- baseline_bundle:
  =artifacts/task6/baselines/tiny-stories-1m-baseline-float-selftest-all-memory-utilization=

** Interpretation
- The main completion path is DDR3-backed TinyStories-1M inference on YPCB.
- =nix build= should represent the latest passing milestone.
- =nix build .#bottleneck= should represent the current unsolved DDR3/inference
  milestone.
- Board claims require JSON/run artifacts under =artifacts/task6/runs/=.

** Acceptance criteria
*** G1 latest green
- [ ] =nix build= succeeds.
- [ ] =nix build .#latest-passing-milestone= succeeds.
- [ ] =just task6-latest-passing-milestone-board-gate= passes or records a
  precise hardware failure signature.
- [ ] =latest_green_artifact= points at the newest accepted PASS artifact.

*** G2 bottleneck
- [ ] =nix build .#bottleneck= succeeds.
- [ ] =just task6-bottleneck-board-gate= runs with an explicit run root.
- [ ] =current_gap= states the exact failing delta from latest green.
- [ ] =next_action= is a single concrete command or diagnostic step.

** Machine-readable view
- JSON companion: [[file:task6-crisp-state.json]]
