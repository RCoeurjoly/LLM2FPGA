* LLM2FPGA Task 6 State of the Art (Canonical)
** Snapshot (YYYY-MM-DD, Europe/Madrid)
- Date: 2026-06-17
- Milestone: task6Int8V9984L2ResidualAddOutputHeadSelftestJtagDebug5MHzBitstream
- Build variant: default flake package (`.#latest-passing-milestone`) + `task6-latest-passing-milestone-board-gate`
- Next milestone: task6-m2 live-context full-block corrected (next major gating target)
- Latest green artifact: unknown (builds exist; board verification not yet green)
- Current gap: board-gate path is not yet reproducibly passing because loader never enters ready state
- Failure signature: `TimeoutError loader did not become ready: magic_ok=True version=13 calib_seen=True state=12 ack=14071040 err=14071040 loader_error=False debug1=0xe500e500` from `task6_ypcb_tinystories_inference_gate.py` after flashing the latest package
- Next action: verify hardware target alignment (board mode/connection/reset), confirm expected reset/recovery path for loaded bitstream, then rerun gate with explicit recovery and a logged run root
- Status: Goal 1.1 is *blocked by hardware path validation*, not by Nix build
- Why DDR3 appears now: `latest-passing-milestone` currently aliases `task6Int8V9984L2ResidualAddOutputHeadSelftestJtagDebug5MHzBitstream`, which is DDR3/JTAG-calibrating path. If this milestone is not intended to include DDR3, add a separate explicit non-DDR3 alias and gate target.

** Acceptance criteria (targeted)
*** Goal 1.1 (build + board program/test)
- [ ] `nix build` for default/`latest-passing-milestone` remains green
- [ ] `just task6-latest-passing-milestone-board-gate` reaches post-calibration and returns PASS
- [ ] Run output includes a PASS artifact in `artifacts/task6/runs/.../`
- [ ] Failure signature for board gate no longer indicates loader timeout

*** Goal 1.2 (next unsolved milestone)
- [ ] Define `.#bottleneck` (or equivalent) in `flake.nix` and ensure it builds
- [ ] Run the gate path with explicit run root
- [ ] Record exact failing delta vs prior green in the state file and next action line

** Machine-readable view
- JSON companion: [[file:task6-crisp-state.json]]
