* LLM2FPGA Task 6 State of the Art
** Snapshot
- last_updated: 2026-06-18T16:17:10+02:00
- milestone: M1-transformer-boundary-mlp
- build_variant: =default/latest-passing-milestone is .#task6-ypcb-pcie-rowstream-ingress-m1-pnr100-bitstream; bottleneck is .#task6-ypcb-pcie-rowstream-ingress-dummy-pnr100-bitstream with M2 enabled and M1 disabled.=
- next_milestone: M2-full-block-board-gate
- latest_green_artifact:
  =artifacts/task6/runs/task6-latest-passing-milestone/mlp-accel-full-output.json=
- current_gap: M1 is freshly HIL-proven and promoted to
  default/latest-passing. M2 builds, flashes, recovers to =pcie_ready=, and
  exposes stable M2 registers, but the M2 full-block board gate fails before
  start because the BAR input readback does not match the requested
  token/control vector.
- failure_signature: =Latest M1 gate: artifacts/task6/runs/task6-latest-passing-milestone/mlp-accel-full-output.json status PASS, state_name DONE, output_valid=true, checksum=0x00001eec, sample0=0xde10ff3e, sample1=0xfd3eed6c, mismatch_count=0. M2 bottleneck gate: artifacts/task6/runs/task6-bottleneck/m2-full-block-board-summary.json status FAIL, failure='M2 input/residual BAR readback did not match before start', m2_present=true, m2_magic=true, m2_context_fixture_signature=true, input_readback=false, input_readback_transform=pcie7x-64bit-rol1, residual_readback=true, start_count_before=0, start_count_after=0, state_name=IDLE, output_valid=false, checksum/sample/output zero. M2 flash/recover evidence: artifacts/task6/runs/2026-06-18T16-05-47+0200-task6-m2-bottleneck-pnr100-flash and artifacts/task6/runs/2026-06-18T16-12-56+0200-task6-m2-bottleneck-recover.=
- next_action: Debug the M2 BAR write/readback path for the token/control input
  vector: explain or fix the =pcie7x-64bit-rol1= transform so the gate can
  assert =start_count_incremented= and leave =IDLE=.
- status: M1_HIL_GREEN_M2_BOTTLENECK
- priority: Use the HIL-proven M1 baseline as the default/latest-passing
  milestone; pursue M2 full-block HIL as the bottleneck toward TinyStories-1M
  inference.
- baseline_bundle:
  =artifacts/task6/baselines/tiny-stories-1m-baseline-float-selftest-all-memory-utilization=

** Interpretation
- The main completion path is still TinyStories-1M inference on YPCB.
- =nix build= now represents the HIL-proven M1 latest-passing milestone.
- =nix build .#bottleneck= represents the current unsolved M2 HIL milestone.
- Board claims require JSON/run artifacts under =artifacts/task6/runs/=.

** Acceptance criteria
*** G1 latest green
- [X] =nix build= succeeds.
- [X] =nix build .#latest-passing-milestone= succeeds.
- [X] =just task6-latest-passing-milestone-board-gate= equivalent passes or records a
  precise hardware failure signature.
- [X] =latest_green_artifact= points at the newest accepted PASS artifact.

*** G2 bottleneck
- [X] =nix build .#bottleneck= succeeds.
- [X] =just task6-bottleneck-board-gate= equivalent ran with explicit output.
- [X] =current_gap= states the exact failing delta from latest green.
- [X] =next_action= is a single concrete command or diagnostic step.

** Machine-readable view
- JSON companion: [[file:task6-crisp-state.json]]
