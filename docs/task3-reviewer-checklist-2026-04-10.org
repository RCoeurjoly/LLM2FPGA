* Task 3 Reviewer Checklist (2026-04-10)

Use this file for reviewer-facing Task 3 verification.

** Scope check

The reviewer should confirm that the Task 3 claim matches
`docs/task3-brief-2026-04-10.org`.

The claim is:
- standard-path lowering of one real LLM
- explicit downstream resource or blocker evidence
- no hidden blackboxes or text-rewrite shortcuts in the canonical path

The claim is not:
- guaranteed FPGA fit
- guaranteed `nextpnr-xilinx` success
- a completed oversized-network execution strategy

** Canonical checks

1. Frontend and SV emission

#+begin_src bash
nix build .#tiny-stories-1m-baseline-float-sv -L
#+end_src

Expected result:
- succeeds
- emits `sources.f`
- emits `sv/main.sv`
- if float extern support is used, emits the explicit appended primitive file

2. Yosys RTLIL handoff

#+begin_src bash
nix build .#tiny-stories-1m-baseline-float-il -L --no-link
#+end_src

Acceptable result:
- either emits RTLIL
- or fails explicitly with a reproducible Yosys/tool-capacity blocker

Not acceptable:
- hidden unresolved modules
- hidden blackboxing
- silent route substitution

3. Yosys stat/report handoff

#+begin_src bash
nix build .#tiny-stories-1m-baseline-float-yosys-stat -L --no-link
#+end_src

Acceptable result:
- either emits a Yosys stat JSON report
- or fails explicitly with a reproducible Yosys/tool-capacity blocker

** Current known state on this branch

As of 2026-04-11 on the working host used for development:
- `tiny-stories-1m-baseline-float-sv` succeeds
- `tiny-stories-1m-baseline-float-il` succeeds and emits RTLIL
- `tiny-stories-1m-baseline-float-yosys-stat` succeeds and emits a Yosys stat
  report
- that report records:
  - `467` SV files
  - `6,971,927` lines in `main.sv`
  - `12,738,203` design cells
  - `327` design memories
  - `433,040,128` total memory bits
  - `411,705,344` bits from the four largest `3216448 x 32` memories alone

This means the current reviewer-facing Task 3 path reaches emitted RTLIL and a
real Yosys resource report without hidden blackboxes or text-rewrite shortcuts.

** Reviewer questions

The reviewer should be able to answer yes to all of the following:

- Is the model a real TinyStories-1M path, not a handwritten surrogate model?
- Is the frontend based on standard PyTorch plus standard compiler tooling?
- Are any temporary stubs or externs explicit and documented?
- Is the first downstream blocker explicit and reproducible?
- Does the repo avoid presenting hidden or alternate paths as the canonical
  Task 3 claim?

If the answer to any of these is no, the Task 3 reviewer story is not yet
clean enough.
