Continue cleaning Task 3 / RfP in LLM2FPGA against the Task 3 contract in
docs/task3-rfp-contract-2026-03-27.org.

Primary gate:
- Task 3 is not complete unless `nix build .#tiny-stories-1m-utilization -L`
  succeeds on the current tree.
- Treat any failure of that command as the main blocker.
- Do not declare success based only on docs, scripts, or intermediate artifacts
  such as `tiny-stories-1m-sv`, `tiny-stories-1m-il`, or
  `tiny-stories-1m-utilization-json`.

Canonical claimed path:
- `tiny-stories-1m-sv -> tiny-stories-1m-il -> tiny-stories-1m-utilization-json -> tiny-stories-1m-utilization`
- This is the only Task 3 utilization claim.
- Remove, demote, or clearly mark any competing Task 3 utilization path as
  non-canonical or historical only.

Task 3 requirements:
- no black boxes
- no stub modules or stub behavior in the canonical path
- no unsupported floating-point operations in the canonical claim
- emitted SystemVerilog must be synthesizable
- use only standard core tools: torch-mlir, mlir, CIRCT, yosys/yosys-slang
- no perl/sed/ad hoc text rewriting in the canonical path
- no non-standard ad hoc PyTorch preprocessing in the canonical path
- nextpnr-xilinx not handling oversized designs is acceptable; Yosys-based
  utilization is sufficient for Task 3

Execution policy:
- first reproduce the canonical gate failure if it is not already freshly
  reproduced
- then fix the highest upstream blocker on the canonical path
- prefer local fast iteration over Nix rebuilds when possible
- after each meaningful fix, rerun the canonical gate
- if the gate still fails at the end of the session, report the exact failing
  derivation and log line, and leave the tree in the best reduced-blocker state
- do not present the task as done unless
  `nix build .#tiny-stories-1m-utilization -L` exits successfully
- if time runs out first, report `NOT DONE` and name the exact blocker

Work autonomously for up to 1 hour. Do not ask questions unless a destructive
change or an unavoidable ambiguity blocks progress.

Priority order:
1. make `nix build .#tiny-stories-1m-utilization -L` pass
2. remove or demote competing Task 3 paths
3. align flake, scripts, and docs to the single canonical utilization path
4. eliminate blackbox/stub behavior in that path
5. identify and reduce remaining float-related blockers
6. keep reviewer milestones reproducible and aligned with the canonical path
